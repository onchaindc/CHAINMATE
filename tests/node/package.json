{
  "//": "Scopes ESM semantics to the Node test files only — the root package.json must stay typeless so Next.js keeps treating .js as CommonJS.",
  "type": "module",
  "scripts": {
    "test": "TOURNAMENT_ROUND_INTERMISSION_MS=0 bun test ."
  }
}
